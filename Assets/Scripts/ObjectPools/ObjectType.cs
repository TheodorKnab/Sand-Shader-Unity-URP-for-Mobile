using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Each object gets a type (an interger "ID"), defined in the GameManager
// Saving is easier that way

public class ObjectType : MonoBehaviour
{
    public GameManager.GardenObjects type;
}
